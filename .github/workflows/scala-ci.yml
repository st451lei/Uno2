name: Scala DesignPattern2

on:
  push:
    branches: [ CI, DesignPattern2 ]
  pull_request:
    branches: [ CI, DesignPattern2 ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 17

      - name: Cache sbt
        uses: actions/cache@v3
        with:
          path: |
            ~/.sbt
            ~/.ivy2/cache
            ~/.coursier
            ~/.cache/coursier
          key: ${{ runner.os }}-sbt-cache

      - name: Run tests
        run: sbt test



